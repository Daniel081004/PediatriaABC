<Window x:Class="PediatriaABC.Views.PrincipalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PediatriaABC.Views"
        mc:Ignorable="d"
        Title="PrincipalView" Height="450" Width="800">
    <StackPanel Background="#E9D5DA">
        <TextBlock FontSize="60" TextAlignment="Center" Background="#8FBDD3" FontFamily="Comic Sans MS" Foreground="#EDE6DB" FontWeight="Bold">Pediatria ABC</TextBlock>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <CheckBox Grid.Column="0" VerticalAlignment="Center" Margin="20,0,0,0" FontSize="20" VerticalContentAlignment="Center">Citas de hoy</CheckBox>

            <DockPanel Grid.Column="1" HorizontalAlignment="Right" Margin="0,5,20,0">
                <Button Margin="5" DockPanel.Dock="Left" Background="Transparent" Width="40" BorderBrush="Transparent">
                    <Image Source="https://cdn-icons-png.flaticon.com/128/3462/3462172.png"></Image>
                </Button>
                <Button Margin="5" DockPanel.Dock="Left" Background="Transparent" Width="45" BorderBrush="Transparent">
                    <Image Source="https://cdn-icons-png.flaticon.com/128/9308/9308015.png"></Image>
                </Button>
                <Button Margin="5" Background="Transparent" Width="40" BorderBrush="Transparent">
                    <Image Source="https://cdn-icons-png.flaticon.com/128/747/747386.png"></Image>
                </Button>
            </DockPanel>
        </Grid>

        <DataGrid Margin="10,5,10,5" CanUserAddRows="False" SelectionMode="Single" AutoGenerateColumns="False">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center"></Setter>
                    <Setter Property="Background" Value="#9D5353"></Setter>
                    <Setter Property="BorderBrush" Value="White"></Setter>
                    <Setter Property="BorderThickness" Value="1"></Setter>
                    <Setter Property="Foreground" Value="#f4f4f4"></Setter>
                    <Setter Property="FontSize" Value="20"></Setter>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Foreground" Value="Black"></Setter>
                    <Setter Property="Background" Value="#EDE6DB"></Setter>
                    <Setter Property="FontSize" Value="15"></Setter>
                    <Setter Property="HorizontalAlignment" Value="Stretch"></Setter>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Header="Nombre del tutor" Width="*"></DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Nombre del hijo" Width="*"></DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Fecha de Nacimiento" Width="Auto"></DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Telefono" Width="Auto"></DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Direccion" Width="Auto"></DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Fecha de registro" Width="Auto"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</Window>